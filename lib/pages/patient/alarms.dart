import 'package:flutter/material.dart';

class PatientAlarm extends StatelessWidget {
  PatientAlarm({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Test"),
    );
  }
}
